! function () {
    const e = document,
        t = e.documentElement,
        n = e.body,
        i = e.getElementById("lights-toggle"),
        s = window.sr = ScrollReveal();

    function a() {
        let d = document.getElementById("lights-toggle")
        localStorage.setItem("dark", i.checked)
        let e = i.parentNode.querySelector(".label-text");
        i.checked ? (n.classList.remove("lights-off"), e && (e.innerHTML = "light"), d.checked = true) : (n.classList.add("lights-off"), e && (e.innerHTML = "dark"))
    }

    function b() {
        let getLocal = localStorage.getItem("dark")
        let e = document.getElementById("lights-toggle").parentNode.querySelector(".label-text");
        getLocal == 'true' ? (document.body.classList.remove("lights-off"), e && (e.innerHTML = "light")) : (document.getElementById("lights-toggle").checked = false, document.body.classList.add("lights-off"), e && (e.innerHTML = "dark"))
    }
    t.classList.remove("no-js"), t.classList.add("js"), window.addEventListener("load", function () {
        n.classList.add("is-loaded")
    }), n.classList.contains("has-animations") && window.addEventListener("load", function () {
        s.reveal(".feature", {
            duration: 600,
            distance: "20px",
            easing: "cubic-bezier(0.215, 0.61, 0.355, 1)",
            origin: "right",
            viewFactor: .2
        })
    }), i && (window.addEventListener("load", b), i.addEventListener("change", a))
}();